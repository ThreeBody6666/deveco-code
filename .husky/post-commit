#!/bin/sh
if ! command -v bun >/dev/null 2>&1; then
  echo "[post-commit] bun not found, skipping desktop build. Install bun to enable auto-build."
  exit 0
fi

echo "[post-commit] building desktop..."
cd packages/desktop || exit 1
bun run install:local
